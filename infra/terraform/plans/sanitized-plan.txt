Sanitized representative plan summary.

Terraform is authored to express the AWS production mapping and is not applied for this assessment.

Expected production-shaped resources:

  + aws_ecr_repository.bank-account-service
  + aws_ecr_lifecycle_policy.bank-account-service
  + aws_eks_cluster.bank-platform
  + aws_eks_node_group.bank-platform-default
  + aws_db_subnet_group.bank-account-service
  + aws_security_group.bank-account-service-postgres
  + aws_db_instance.bank-account-service
  + aws_secretsmanager_secret.bank/prod/account-service/database
  + aws_sqs_queue.bank-account-service-events
  + aws_sqs_queue.bank-account-service-events-dlq
  + aws_iam_role.bank-account-service
  + aws_iam_policy.bank-account-service
  + aws_cloudwatch_log_group./aws/eks/bank-account-service

Important security intent:

  ~ RDS storage encrypted and deletion protection enabled
  ~ RDS IAM database authentication enabled
  ~ Workload IAM policy scoped to the database secret, queue, and bank_account_app rds-db:connect permission

No real account IDs, ARNs, generated passwords, or secret values are included.
